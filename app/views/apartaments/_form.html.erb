<%= simple_form_for(@apartament) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :adres %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
